#include "main.h"

/**
* _abs - computes the absolute value of an integer
* @x: The character to print
*
* Return: absolute value
* Otherwise return 0.
*/
int _abs(int x)
{
	return (x * ((x < 0) * (-1) + (x > 0)));
	return (0);
}
